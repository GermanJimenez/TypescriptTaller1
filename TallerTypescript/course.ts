

export class Course {
  
    constructor(public id:number, public name: string,  public channel: string, public seassons:number,
      public description:string, public page:string, public photo:string
     ) {
    
  }
}
